# 压缩模块 zipfile

import zipfile
import os

# 压缩文件 操作
# with zipfile.ZipFile('G:/Code/-.test/spam.zip','w') as myzip:
#     myzip.write('1.txt')
#     myzip.write('data.txt')

# 解压文件 操作
# with zipfile.ZipFile('G:/Code/-.test/spam.zip','r') as myzip:
#     myzip.extractall('./')  # 带 all 提取全部内容

# 压缩当前文件夹中的所有文件？
# with zipfile.ZipFile('spam.zip', 'w') as myzip:
#     arr = os.listdir('./')
#     for i in arr:
#         print(i)
#         myzip.write(i)


























































