# 字符串查找相关函数 find、index、count

# vars = 'iloveyoutosimidaandilikeyou'
# 1. 检测一个字符串是否存在于一个字符串中
# res = 'love' in vars
# 2. 获取字符串的长度 len()
# res = len(vars)
# 3. str.find(sub[,start[,end]])    从左向右 获取指定字符在字符串中第一次出现的索引位置
#                                   未找到则返回 -1
# res = vars.find('you',10,27)
# 4. str.rfind()                    从右向左 获取指定字符在字符串中第一次出现的索引位置
#                                   未找到则返回 -1
# res = vars.rfind('you',1,10)
# 5. str.index() 和 find方法 一样，如果没有找到则报错  ValueError: substring not found
# res = vars.index('yous')
# res = vars.rindex('yous')
# 6. str.count(sub[,start[,end]])   统计字符串：检测一个字符串在指定字符串中出现的次数
# res = vars.count('i')
# print(res)

# 作业：代码直接敲 三遍，单词全部背下来



















































